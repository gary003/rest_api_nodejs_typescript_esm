name: CI/CD Pipeline

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

env:
  NODE_VERSION: 24
  DOCKER_IMAGE: gary003/rest_api_nodejs_typescript_esm
  DOCKER_TAG: latest

jobs:
  security-scan:
    runs-on: ubuntu-latest
    permissions: write-all
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install dependencies
        run: npm ci

      # Static Analysis - Trivy
      - name: Trivy filesystem scan
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          scan-ref: '.'
          format: 'table'
          exit-code: '0'

      - name: Trivy filesystem scan (SARIF)
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          scan-ref: '.'
          format: 'sarif'
          output: 'trivy-results.sarif'

      # Build Docker image
      - name: Build Docker image
        run: npm run docker:build:image

      # Container Scanning - Trivy
      - name: Trivy container scan
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: '${{ env.DOCKER_IMAGE }}:${{ env.DOCKER_TAG }}'
          format: 'table'
          exit-code: '0'

      - name: Trivy container scan (SARIF)
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: '${{ env.DOCKER_IMAGE }}:${{ env.DOCKER_TAG }}'
          format: 'sarif'
          output: 'trivy-container.sarif'

      # Start application for DAST
      - name: Start application
        run: npm run start --wait && echo "Application started successfully" || exit 1
        env:
          NODE_ENV: ${{ env.NODE_ENV }}
          API_VERSION: ${{ env.API_VERSION }}
          API_SCHEME: ${{ env.API_SCHEME }}
          API_HOST: ${{ env.API_HOST }}
          API_PORT: ${{ env.API_PORT }}
          API_BASEPATH: ${{ env.API_BASEPATH }}
          LOGLEVEL: ${{ env.LOGLEVEL }}
          DB_DRIVER: ${{ env.DB_DRIVER }}
          DB_HOST: ${{ env.DB_HOST }}
          DB_URI: ${{ env.DB_URI }}
          DB_PORT: ${{ env.DB_PORT }}
          DB_USERNAME: ${{ env.DB_USERNAME }}
          DB_PASSWORD: ${{ env.DB_PASSWORD }}
          DB_DATABASE_NAME: ${{ env.DB_DATABASE_NAME }}
          DB_ROOT_PASSWORD: ${{ env.DB_ROOT_PASSWORD }}
          JWT_SECRET_KEY: ${{ env.JWT_SECRET_KEY }}
          JWT_REFRESH_SECRET_KEY: ${{ env.JWT_REFRESH_SECRET_KEY }}
          REDIS_URL: ${{ env.REDIS_URL }}
          CRYPTO_SECRET_KEY: ${{ env.CRYPTO_SECRET_KEY }}
          # OTEL_TRACE_EXPORTER_ENDPOINT: ${{ env.OTEL_TRACE_EXPORTER_ENDPOINT }}
          # OTEL_METRIC_EXPORTER_ENDPOINT: ${{ env.OTEL_METRIC_EXPORTER_ENDPOINT }}
          # GF_AUTH_ANONYMOUS_ENABLED: ${{ env.GF_AUTH_ANONYMOUS_ENABLED }}
          # GF_AUTH_ANONYMOUS_ORG_ROLE: ${{ env.GF_AUTH_ANONYMOUS_ORG_ROLE }}

      # Dynamic Analysis - OWASP ZAP
      - name: OWASP ZAP Baseline Scan
        uses: zaproxy/action-baseline@v0.15.0
        with:
          target: 'http://localhost:8080/api'
          rules_file_name: '.zap/rules.tsv'
          cmd_options: '-a -j'
          fail_action: false
          artifact_name: 'zap-scan-results'
        continue-on-error: true

      # Upload ZAP reports manually with unique name
      - name: Upload ZAP Scan Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: zap-baseline-report-${{ github.run_number }}
          path: |
            report_html.html
            report_json.json
            report_md.md
          retention-days: 7
          if-no-files-found: warn

      # Show application logs if ZAP scan fails
      - name: Show application logs on failure
        if: failure()
        run: |
          echo "=== Application Logs ==="
          docker compose logs app
          echo ""
          echo "=== Database Logs ==="
          docker compose logs db

      # Stop containers
      - name: Stop containers
        if: always()
        run: docker compose down -v

      # Upload all results to GitHub Security
      - name: Upload Trivy filesystem results
        if: always()
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: 'trivy-results.sarif'
          category: 'trivy-fs'
        continue-on-error: true

      - name: Upload Trivy container results
        if: always()
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: 'trivy-container.sarif'
          category: 'trivy-container'
        continue-on-error: true

      # Check for high severity issues (warning only, don't fail)
      - name: Check for high severity issues
        if: always()
        continue-on-error: true
        run: |
          if [ -f trivy-results.sarif ] && [ -f trivy-container.sarif ]; then
            if grep -q "HIGH\|CRITICAL" trivy-results.sarif trivy-container.sarif; then
              echo "⚠️  High/Critical vulnerabilities found - Review required"
              echo "Check GitHub Security tab for details"
            else
              echo "✅ No High/Critical vulnerabilities found"
            fi
          fi

  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run Lint
        run: npm run lint

  test-app:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Create .env file
        run: |
          echo "DB_DRIVER=mysql" >> .env
          echo "DB_HOST=localhost" >> .env
          echo "DB_PORT=3306" >> .env
          echo "DB_USERNAME=root" >> .env
          echo "DB_PASSWORD=testpassword" >> .env
          echo "DB_DATABASE_NAME=testdb" >> .env
          echo "DB_URI=mysql://root:testpassword@localhost:3306/testdb" >> .env
          echo "API_PORT=8080" >> .env
          echo "LOGLEVEL=debug" >> .env
          echo "NODE_ENV=test" >> .env
          echo "SWAGGER_HOST=localhost:8080" >> .env
          echo "SWAGGER_BASE_PATH=/api" >> .env

      - name: Run all tests
        run: npm run test:ci
        env:
          CI: true

      - name: Upload test artifacts (if failed)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-logs
          path: |
            *.log
            coverage/

  docker-build-image:
    runs-on: ubuntu-latest
    needs: test-app
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        run: npm run docker:build:image

      - name: Test Docker image
        run: |
          docker run --rm -d -p 8080:8080 --name test-container ${{ env.DOCKER_IMAGE }}:${{ env.DOCKER_TAG }}

          # Wait for container to be ready
          timeout=30
          counter=0
          until curl -sf http://localhost:8080/api/health > /dev/null || [ $counter -eq $timeout ]; do
            echo "Waiting for container... ($counter/$timeout)"
            sleep 2
            counter=$((counter + 2))
          done

          if [ $counter -eq $timeout ]; then
            echo "Container failed to start"
            docker logs test-container
            docker stop test-container
            exit 1
          fi

          echo "Container is ready"
          docker stop test-container

  deploy-dockerHub:
    runs-on: ubuntu-latest
    needs: docker-build-image
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}
          logout: true

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ env.DOCKER_IMAGE }}:${{ env.DOCKER_TAG }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
